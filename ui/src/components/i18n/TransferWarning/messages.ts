import { defineMessages } from "react-intl"

export default defineMessages({
  warning: "Warning",
  readRules: "To prevent permanent asset loss, please read the bridge rules.",
  noExchangeAddress:
    "Do not send tokens to a centralized exchange’s deposit address.",
})
